getwd()
## Import IMDB score talbe into variable called "movie"
movie = read.csv("movies.csv")
View(movie)
## with the help of proper packages
install.packages("readxl")
library(readxl)
movie3 = read_excel("movies.xlsx", sheet = 3)
View(movie3)
ramen_ratings <- readr::read_csv("https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2019/2019-06-04/ramen_ratings.csv")
View(ramen_ratings)
View(movie3)
rm(ramen_ratings)
load('~/Downloads/rotten.RData')
View(movies)
View(movie)
View(movies)
summary(movies$year)
rm(movies)
max(movie$Year)
max(movie$Year, na.rm = T)
netflix = read.csv('../data/netflix.csv')
View(netflix)
View(movie)
library(tidyverse)
moviet = separate(movie, Genres)
moviet = separate(movie, Genres, sep = '|')
moviet = separate(movie, Genres, sep = '|', into = NA)
moviet = separate(movie, Genres, sep = '|', into = c('a', 'b', 'c', 'd'))
View(moviet)
moviet = separate(movie, Genres, sep = '|', into = c('a', 'b'))
View(moviet)
moviet = separate(movie, Genres, sep = '\|', into = c('a', 'b'))
moviet = separate(movie, col = Genres, into = c('a', 'b', NA), sep = '|', extra = 'merge')
View(moviet)
movie$Genres[1]
movie$Genres %>% class()
movie = read.csv('movies.csv', stringsAsFactors = F)
moviet = separate(movie, col = Genres, into = c('a', 'b', NA), sep = '|', extra = 'merge')
View(moviet)
movie$Genres[1]
str_split(movie$Genres[1], '|')
str_split(movie$Genres[1], '\|')
str_split(movie$Genres[1])
str_split(movie$Genres[1], '\\|')
moviet = separate(movie, col = Genres, into = c('a', 'b', 'c'), sep = '\\|', extra = 'merge')
View(moviet)
moviet = separate(movie, col = Genres, sep = '\\|')
moviet = separate(movie, col = Genres, sep = '\\|', into = NA)
View(movie3)
View(iris3)
View(iris)
liris = gather(iris, key = 'Measure', value = 'Value')
View(liris)
liris = gather(iris, key = 'Measure', value = 'Value', -Species)
View(liris)
liris = gather(iris, key = 'Measure', value = 'Value', -c(Species, Petal.Width))
View(mtcars)
View(mpg)
rm(list = ls())
movie.list = list()
for(i in 1:3){movie.list[[i]] = read_excel('movies.xlsx', sheet = i)}
movie.list
movie.all = rbind(movie.list[[1]], movie.list[[2]], movie.list[[3]])
View(movie.all)
## Stack all the sheets together through function rbind and for loop
movie.all = c()
for(i in 1:3){movie.all = rbind(movie.all, read_excel('movies.xlsx', sheet = i))}
excel_sheets('movies.xlse')
excel_sheets('movies.xlsx')
movie.list = list()
for(i in 1:3){movie.list[[i]] = read_excel('movies.xlsx', sheet = i)}
View(movie.list)
